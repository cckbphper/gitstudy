<van-tabbar active="{{ selected }}" bind:change="onChange">
  <van-tabbar-item icon="{{item.icon}}" wx:for="{{list}}" wx:key="index" data-path="{{item.pagePath}}" data-index="{{index}}" bindtap="switchTab">{{item.text}}</van-tabbar-item>
</van-tabbar>

